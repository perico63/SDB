MÓDULO CONTROLE

Primeiramente devemos executar a classe servidor que está no pacote 
controle que está no projeto servidor.

Na sequencia executa-se a classe login que está no pacote IU no projeto
Client.

Deve ser fornecido o IP 127.0.0.1 e Porta 4321
usuario = usuario
senha = senha

Modulo de Sorteio

Para utilizar esse módulo basta selecionar a opcao de sorteio, em seguida escolher o evento, para finalizar basta clicar no botão sortear e será apresentado o nome do vencedor.
